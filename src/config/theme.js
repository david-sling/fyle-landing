import { createTheme } from "@material-ui/core";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff3147",
    },
    secondary: {
      main: "#ff3147",
    },
  },
});

export default theme;
